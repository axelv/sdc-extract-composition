[project]
name = "viewer-backend"
version = "0.1.0"
description = "FastAPI backend for SDC Composition template rendering"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.34",
    "fhirpathpy>=1.1",
]
